import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Badge } from "@/components/ui/badge"
import { Progress } from "@/components/ui/progress"
import {
  GraduationCap,
  CalendarDays,
  Clock,
  Building2,
  Hash,
  BookOpen,
  TrendingUp,
} from "lucide-react"

/**
 * Card principal do painel do aluno.
 * Exibe: identificação, curso, ciclo de matrícula, empresa,
 * período do estágio e progresso da carga horária.
 */
export default function CardPrincipal() {
  return null // será implementado no painel
}